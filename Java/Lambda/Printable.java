package Java.Lambda;

@FunctionalInterface                // here Printable java_.OOPs.Interface has a single abstract method, we can say FunctionalInterface
public interface Printable {
    void print(String prefix,String suffix);
}
